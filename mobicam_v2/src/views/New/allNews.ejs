<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>all tin tuc</title>
    <%- include('../partials/head.ejs') %>
</head>
<body>
    <div class="wrapper wrapper_new_page">
        <%- include('../partials/header.ejs') %>
        <section class="news pt-3 pb-5">
            <div class="container">
              <h2 class="text-primary text-center fw-bold mb-2">Tin tức</h2>
              <div class="wrapper_dot">
                <div class="dot"></div>
              </div>
              <div class="content mt-4">
                <div class="row">
                  <% console.log(news) %> <% for (let i = 0; i < news.length; i++) {
                  %>
                  <div class="col-lg-4 col-md-6 col-sm-12 mb-3">
                    <a
                      href="/news/detail/<%= news[i].id %>"
                      class="item rounded d-block text-decoration-none overflow-hidden"
                    >
                      <div style="height: 215px" class="thumb_new overflow-hidden">
                        <img src="<%= news[i].img %>" alt="thumb_new" />
                      </div>
                      <div
                        class="title fw-bold pb-5 pt-2 ps-3 pe-3 text-decoration"
                      >
                        <%= news[i].title %>
                      </div>
                    </a>
                  </div>
                  <% } %>

                </div>
              </div>
            </div>
          </section>
        <%- include('../partials/footer.ejs') %>
    </div>
</body>
</html>